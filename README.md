# GreatPros Demand Analysis
Inputs data from Lead Capsule and outputs the demand for home service jobs(HITs) in cities around the country. Completed project first in Python, then Java. Uses Sqlite DBMS.
